{
  "name": "brightpath",
  "private": true,
  "version": "0.2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w @brightpath/shared && npm run build -w @brightpath/i18n && concurrently -n shared,i18n,web,api -c yellow,cyan,blue,green \"npm run dev -w @brightpath/shared\" \"npm run dev -w @brightpath/i18n\" \"npm run dev -w @brightpath/web\" \"npm run dev -w @brightpath/api\"",
    "dev:web": "npm run dev -w @brightpath/web",
    "dev:api": "npm run dev -w @brightpath/api",
    "build": "npm run build -w @brightpath/shared && npm run build -w @brightpath/i18n && npm run build -w @brightpath/api && npm run build -w @brightpath/web",
    "remotion:studio": "npm run studio -w @brightpath/remotion",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "db:migrate": "npm run db:migrate -w @brightpath/api",
    "db:push": "npm run db:push -w @brightpath/api",
    "migrate:monorepo": "node scripts/migrate-monorepo.mjs"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "overrides": {
    "zod": "4.4.3"
  }
}
