-- Activity tracking fields on Parent
ALTER TABLE "Parent"
  ADD COLUMN IF NOT EXISTS "currentStreak" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "longestStreak" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "lastActiveDate" TIMESTAMP(3),
  ADD COLUMN IF NOT EXISTS "timeStudiedThisWeek" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "lastWeekResetTimestamp" TIMESTAMP(3);
